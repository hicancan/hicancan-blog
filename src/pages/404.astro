---
import Layout from '../layouts/Layout.astro';
import { SITE_CONFIG } from '../config';
import IconArrowLeft from '../components/icons/IconArrowLeft.astro';
---

<Layout title="404 Not Found" nebulaVariant="none">
    <div class="flex flex-col items-center justify-center min-h-[70vh] text-center relative z-10">
        <!-- Background Glow -->
        <div
            class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-96 h-96 bg-primary/20 rounded-full blur-[128px] pointer-events-none"
        >
        </div>

        <h1
            class="text-[12rem] font-black text-transparent bg-clip-text bg-gradient-to-r from-primary/20 to-accent/20 select-none leading-none"
        >
            {SITE_CONFIG.ui.notFound.title}
        </h1>

        <div
            class="relative -mt-12 backdrop-blur-md border border-white/10 bg-white/5 rounded-2xl p-8 shadow-2xl"
        >
            <h2 class="text-2xl font-bold text-white mb-2">
                {SITE_CONFIG.ui.notFound.subtitle}
            </h2>
            <p
                class="text-text-secondary font-mono text-sm max-w-sm mx-auto mb-8 whitespace-pre-line"
            >
                {SITE_CONFIG.ui.notFound.message}
            </p>

            <a
                href="/"
                class="inline-flex items-center gap-2 px-6 py-3 bg-white/10 hover:bg-white/20 text-white rounded-full transition-all group border border-white/10 hover:border-accent/50"
            >
                <span>{SITE_CONFIG.ui.notFound.button}</span>
                <IconArrowLeft class="w-4 h-4 group-hover:-translate-x-1 transition-transform" />
            </a>
        </div>
    </div>
</Layout>
